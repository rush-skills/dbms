<%= simple_form_for(@prerequisite) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :prerequisite_id %>
    <%= f.association :course %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
